<nav class="bg-gray-100 h-16 w-full flex flex-row items-center shadow-md pl-8 pr-8 rounded-md">
    <img src="{{ asset('img/logos/logo-placeholder.png') }}" alt="logo" class="h-4/5 rounded-full border-2 border-blue-600  ">
    <h1 class="ml-2 font-bold text-2xl text-teal-600"><span class="text-yellow-500">E</span><span class="text-orange-500">N</span>Ivent</h1>

    {% if app.user %}
        <div class="h-full w-full flex flex-row gap-8 items-center justify-end [&>*:nth-child(3n-2)]:border-yellow-400 [&>*:nth-child(3n-1)]:border-orange-400 [&>*:nth-child(3n)]:border-teal-700">
            <a href="{{ path('main_home') }}" class="w-32 p-2 border-2 rounded hover:text-gray-500"><button class="h-full w-full">Home</button></a>

            {% for role in app.user.roles %}
                {% if role == 'ROLE_ADMIN' %}
                    <a href="{{ path('app_admin') }}" class="w-32 p-2 border-2 rounded hover:text-gray-500"><button class="h-full w-full">Dashboard</button></a>
                {% endif %}
            {% endfor %}

            <a href="{{ path('app_myprofile') }}" class="w-32 p-2 border-2 rounded hover:text-gray-500"><button class="h-full w-full">Profile</button></a>

            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="w-32 p-2 border-2 rounded hover:text-gray-500"><button class="h-full w-full">Logout</button></a>
            {% endif %}
        </div>
    {% endif %}

</nav>